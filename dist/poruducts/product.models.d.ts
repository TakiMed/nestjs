export declare class Product {
    id: string;
    title: string;
    description: string;
    price: number;
    quantity: number;
    constructor(id: string, title: string, description: string, price: number, quantity: number);
}
